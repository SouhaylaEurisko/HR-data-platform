"""Utility modules for the chatbot service.

Import concrete helpers directly from their sub-modules, e.g.::

    from app.utils.pydantic_ai_client import build_agent, run_typed
    from app.utils.db_utils import execute_safe_query
"""
